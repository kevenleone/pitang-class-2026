info:
  name: Patch User
  type: http
  seq: 3

http:
  method: POST
  url: http://localhost:3333/api/users
  body:
    type: json
    data: |-
      {
        "name": "Keven Leone",
        "username": "kevenleone"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
